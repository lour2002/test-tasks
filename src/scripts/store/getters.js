const statistics = state => {
  return state.statistics;
};
const rows = state => {
  return state.rows;
};
const chartData = state => {
  return state.chartData;
};
export default {
  statistics,
  rows,
  chartData
};
